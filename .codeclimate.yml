exclude_paths:
  - "UI"
  - "server/models"
checks:
  return-statements:
    enabled: false
  similar-code:
    enabled: false
  identical-code:
    enabled: false
  file-lines:
    enabled: false
  method-lines:
    config:
      threshold: 40
  method-complexity:
    config:
      threshold: 8
